#!/bin/sh

# Power menu script using tofi

CHOSEN=$(printf "ASM" | rofi -dmenu)

case "$CHOSEN" in
	"ASM") alacritty -e nix develop /etc/nixos/nix-config/home-manager/user/soulofset/dev/asm ;;
	*) exit 1 ;;
esac
